# Helm Chart Repository

To add this repository:

```bash
helm repo add library-management https://masoodzadmehr.github.io/basalam-k8s-image
helm repo update
helm install my-library library-management/library-management
```
